apiVersion: v1
kind: Service
metadata:
  name: task-manager-postgres
  labels:
    app: task-manager-postgres
spec:
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
      name: postgres
  selector:
    app: task-manager-postgres
  type: ClusterIP
